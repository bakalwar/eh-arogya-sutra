# Patient data minimization

- Store structured findings only after verification workflows  
- Never permanently store original photos/PDFs/report bytes  
- Masked contact fields only at persistence foundation  
- Patient name never used in URLs; use `public_id`  
- Phone number is not a primary key  
- Synthetic data only in tests
