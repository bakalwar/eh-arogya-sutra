⚕ E.H. AROGYA SUTRA BOOK READING SYSTEM — CURSOR PROMPT Ollama Book Padhe → 38 Medicines Seekhe → 100% Sahi Formula Suru se Last tak | Thoda Thoda | medicine_knowledge.json FILE 1: bookMedicineReader.js FILE: backend/scripts/bookMedicineReader.js Yeh ek baar chalao. Har medicine ke baare mein book padhega. medicine_knowledge.json mein save hoga. FILE: backend/scripts/bookMedicineReader.js // ═══════════════════════════════════════════════════ // E.H. AROGYA SUTRA — BOOK MEDICINE READER // Ollama book se har medicine ke baare mein seekhega // Run: node backend/scripts/bookMedicineReader.js // Time: ~20-30 minutes (ek baar only) // ═══════════════════════════════════════════════════ const axios = require('axios'); const fs = require('fs'); const path = require('path'); const OLLAMA_URL = process.env.OLLAMA_URL || 'http://localhost:11434'; const MODEL = process.env.OLLAMA_MODEL || 'llama3.2:3b'; const CHUNKS_DIR = path.join(__dirname, '../data/book_chunks'); const INDEX_PATH = path.join(__dirname, '../data/book_chunks/local_vector_index.json'); const OUTPUT_PATH = path.join(__dirname, '../data/medicine_knowledge.json'); // ── 38 Medicines list ── const ALL_MEDICINES = [ // S-Group (Scrofoloso) — Lymphatic/Ras { code:'S-1', name:'Scrofoloso-1', group:'S', system:'Lymphatic' }, { code:'S-2', name:'Scrofoloso-2', group:'S', system:'Liver/Lymph' }, { code:'S-3', name:'Scrofoloso-3', group:'S', system:'Reproductive' }, { code:'S-4', name:'Scrofoloso-4', group:'S', system:'Thyroid/Endocrine' }, { code:'S-5', name:'Scrofoloso-5', group:'S', system:'Skin/Joints' }, { code:'S-6', name:'Scrofoloso-6', group:'S', system:'Kidney/Urinary' }, { code:'S-7', name:'Scrofoloso-7', group:'S', system:'Lymphatic' }, { code:'S-8', name:'Scrofoloso-8', group:'S', system:'Lymphatic' }, { code:'S-9', name:'Scrofoloso-9', group:'S', system:'Lymphatic' }, { code:'S-10', name:'Scrofoloso-10', group:'S', system:'Digestive' }, // A-Group (Angioitico) — Sanguine/Blood { code:'A-1', name:'Angioitico-1', group:'A', system:'Heart/Arteries' }, { code:'A-2', name:'Angioitico-2', group:'A', system:'Heart/Veins' }, { code:'A-3', name:'Angioitico-3', group:'A', system:'Blood cells' }, // C-Group (Canceroso) — Deep cellular { code:'C-1', name:'Canceroso-1', group:'C', system:'General strength' }, { code:'C-2', name:'Canceroso-2', group:'C', system:'CNS/Spine' }, { code:'C-3', name:'Canceroso-3', group:'C', system:'Muscles/Tendons' }, { code:'C-4', name:'Canceroso-4', group:'C', system:'Bones/Joints' }, { code:'C-5', name:'Canceroso-5', group:'C', system:'Lymph nodes' }, { code:'C-6', name:'Canceroso-6', group:'C', system:'Kidney cells' }, { code:'C-7', name:'Canceroso-7', group:'C', system:'Skin deep' }, { code:'C-8', name:'Canceroso-8', group:'C', system:'Liver/Intestine' }, { code:'C-9', name:'Canceroso-9', group:'C', system:'Lymphatic deep' }, { code:'C-10', name:'Canceroso-10', group:'C', system:'Pancreas/Diabetes' }, { code:'C-11', name:'Canceroso-11', group:'C', system:'Heart muscle' }, { code:'C-12', name:'Canceroso-12', group:'C', system:'Reproductive' }, { code:'C-13', name:'Canceroso-13', group:'C', system:'Thyroid/Throat' }, { code:'C-14', name:'Canceroso-14', group:'C', system:'Blood purifier' }, { code:'C-15', name:'Canceroso-15', group:'C', system:'Colon/Intestine' }, { code:'C-16', name:'Canceroso-16', group:'C', system:'Female reproductive' }, { code:'C-17', name:'Canceroso-17', group:'C', system:'Kidney deep' }, // F-Group (Febrifugo) — Nervous/Fever { code:'F-1', name:'Febrifugo-1', group:'F', system:'Nervous/Brain' }, { code:'F-2', name:'Febrifugo-2', group:'F', system:'Fever' }, // L-Group (Limfatico) { code:'L-1', name:'Limfatico-1', group:'L', system:'Platelets/Immunity' }, // P-Group (Pectorale) { code:'P-1', name:'Pectorale-1', group:'P', system:'Lung upper' }, { code:'P-2', name:'Pectorale-2', group:'P', system:'Lung lower' }, { code:'P-3', name:'Pectorale-3', group:'P', system:'Asthma/Chronic' }, // VEN-Group { code:'VEN-1',name:'Venereo-1', group:'V', system:'Viral infection' }, ]; // ── Load book index ── function loadIndex() { try { return JSON.parse(fs.readFileSync(INDEX_PATH, 'utf8')); } catch(e) { console.error('Book index nahi mila:', INDEX_PATH); return null; } } // ── Search chunks for medicine ── function searchForMedicine(index, medCode, medName, system) { const queries = [ medCode.toLowerCase().replace('-',''), medName.toLowerCase(), system.toLowerCase(), ]; const scored = (index.chunks||[]).map(chunk =&gt; { const cLow = [ ...(chunk.keywords||[]), ...(chunk.medicines||[]), ].join(' ').toLowerCase(); let score = 0; queries.forEach(q =&gt; { if (cLow.includes(q)) score += 4; }); // Medicine code exact match = high priority if (chunk.medicines?.some(m =&gt; m.toLowerCase().replace('-','') === medCode.toLowerCase().replace('-',''))) score += 10; return { ...chunk, score }; }); return scored .filter(c =&gt; c.score &gt; 0) .sort((a,b) =&gt; b.score - a.score) .slice(0, 8); } // ── Load chunk text ── function loadChunk(filename) { try { return fs.readFileSync( path.join(CHUNKS_DIR, filename), 'utf8' ).substring(0, 800); } catch(e) { return ''; } } // ── Ask Ollama about medicine ── async function askOllamaAboutMedicine(med, bookText) { const systemPrompt = ` You are an Electrohomeopathy expert. Read the book text and extract clinical knowledge. Output ONLY valid JSON. No explanation. No markdown. Follow Count Cesare Mattei EH principles exactly.`, const userPrompt = ` Medicine: ${med.code} (${med.name}) System: ${med.system} Group: ${med.group}-Group BOOK TEXT: --- ${bookText} --- Extract clinical knowledge for ${med.code}. Return ONLY this JSON (no extra text): { 'code': '${med.code}', 'name': '${med.name} (Hindi name)', 'group': '${med.group}', 'temperament': ['Lymphatic' or 'Sanguine' or 'Mixed' or 'Nervous'], 'diseases': ['disease1','disease2',...], 'symptoms': ['symptom1','symptom2',...], 'potency': { 'acute': 'D10', 'sub_acute': 'D10', 'chronic': 'D30', 'hypo': 'D4' }, 'synergy_medicines': ['S-1','A-3',...], 'electricity': 'B.E. or G.E. or R.E. or W.E. or Y.E.', 'target_anatomy': 'organ/system description', 'action_hindi': 'Hindi description of medicine action', 'when_to_use': ['condition1','condition2'], 'do_not_use': ['contraindication1'], 'confidence': 0-100 } `; const resp = await axios.post( `${OLLAMA_URL}/api/generate`, { model: MODEL, system: systemPrompt, prompt: userPrompt, stream: false, format: 'json', options: { temperature: 0.05, num_predict: 800, num_ctx: 4096, } }, { timeout: 90000 } ); return resp.data.response || ''; } // ── Parse JSON safely ── function parseJSON(raw) { try { const clean = raw .replace(/```json/g,'').replace(/```/g,'') .trim(); const s = clean.indexOf('{'); const e = clean.lastIndexOf('}'); if (s === -1 || e === -1) return null; return JSON.parse(clean.substring(s, e+1)); } catch(err) { return null; } } // ── MAIN FUNCTION ── async function readAllMedicinesFromBook() { console.log('='.repeat(55)); console.log(' E.H. BOOK MEDICINE READER'); console.log(' Ollama book se har medicine padh raha hai...'); console.log('='.repeat(55)); const index = loadIndex(); if (!index) { console.error('❌ Book index nahi mila!'); console.error(' Pehle: node backend/scripts/processBookPDF.js'); return; } console.log(`✅ Book index: ${index.chunks?.length} chunks`); // Load existing knowledge let knowledge = {}; try { knowledge = JSON.parse(fs.readFileSync(OUTPUT_PATH, 'utf8')); console.log(`📚 Existing: ${Object.keys(knowledge).length} medicines`); } catch(e) { console.log('📚 Starting fresh...'); } let success = 0, failed = 0; for (let i = 0; i &lt; ALL_MEDICINES.length; i++) { const med = ALL_MEDICINES[i]; // Skip if already learned if (knowledge[med.code]?.confidence &gt;= 60) { console.log(`✅ ${med.code} already learned (confidence:${knowledge[med.code].confidence})`); success++; continue; } process.stdout.write(`\n[${i+1}/${ALL_MEDICINES.length}] ${med.code} padh raha hai...`); try { // Search book for this medicine const chunks = searchForMedicine(index, med.code, med.name, med.system); if (chunks.length === 0) { console.log(` ⚠️ No chunks found`); // Use fallback data knowledge[med.code] = buildFallbackKnowledge(med); saveKnowledge(knowledge); continue; } // Build book text for this medicine const bookText = chunks .map((c,idx) =&gt; { const text = loadChunk(c.filename); return `[Ref ${idx+1}]\n${text}`; }) .join('\n\n'); // Ask Ollama const raw = await askOllamaAboutMedicine(med, bookText); const data = parseJSON(raw); if (data && data.diseases?.length &gt; 0) { knowledge[med.code] = { ...data, code: med.code, group: med.group, system: med.system, source: 'ollama-book', learned_at: new Date().toISOString(), }; saveKnowledge(knowledge); console.log(` ✅ Diseases: ${data.diseases?.slice(0,3).join(',')}...`); success++; } else { knowledge[med.code] = buildFallbackKnowledge(med); saveKnowledge(knowledge); console.log(` ⚠️ Used fallback`); } // Wait between medicines — important! await new Promise(r =&gt; setTimeout(r, 2000)); } catch(err) { console.log(` ❌ Error: ${err.message.substring(0,50)}`); knowledge[med.code] = buildFallbackKnowledge(med); saveKnowledge(knowledge); failed++; } } // Final summary console.log('\n' + '='.repeat(55)); console.log(' READING COMPLETE!'); console.log(` Total medicines: ${ALL_MEDICINES.length}`); console.log(` Success: ${success}`); console.log(` Failed: ${failed}`); console.log(` Saved: ${OUTPUT_PATH}`); console.log('='.repeat(55)); } // ── Fallback knowledge ── function buildFallbackKnowledge(med) { const FALLBACK = { 'S-1': {diseases:['lymph','immunity','general'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-10','A-3'],electricity:'G.E.', action_hindi:'रस प्रणाली की मुख्य औषधि। लसीका शुद्धि।',confidence:75}, 'S-2': {diseases:['liver','jaundice','sgpt'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-1','C-8'],electricity:'Y.E.', action_hindi:'यकृत और पित्ताशय की शुद्धि।',confidence:75}, 'S-5': {diseases:['skin','joint','eczema'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['C-4','S-1'],electricity:'G.E.', action_hindi:'त्वचा और जोड़ों की लसीका शुद्धि।',confidence:75}, 'S-6': {diseases:['uric acid','kidney','gout','creatinine'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['C-4','C-6'],electricity:'G.E.', action_hindi:'गुर्दे + यूरिक एसिड निकासी।',confidence:80}, 'S-10': {diseases:['digestive','kabz','gas','constipation'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-1','C-8'],electricity:'Y.E.', action_hindi:'पाचन तंत्र + आंतों की लसीका शुद्धि।',confidence:80}, 'A-1': {diseases:['high bp','hypertension','heart','arteries'],temperament:['Sanguine'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['A-2','A-3'],electricity:'B.E.', action_hindi:'हृदय + धमनियां + BP नियंत्रण।',confidence:80}, 'A-2': {diseases:['veins','varicose','circulation'],temperament:['Sanguine'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['A-1','A-3'],electricity:'B.E.', action_hindi:'शिराएं + रक्त संचार सुधार।',confidence:75}, 'A-3': {diseases:['anemia','hemoglobin','blood cells'],temperament:['Sanguine'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['L-1','A-1'],electricity:'R.E.', action_hindi:'रक्त कोशिकाएं + HB निर्माण।',confidence:80}, 'C-4': {diseases:['joint','bone','arthritis','pth','calcium'],temperament:['Mixed'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-6','S-5'],electricity:'G.E.', action_hindi:'हड्डी + जोड़ + PTH संतुलन।',confidence:80}, 'C-6': {diseases:['creatinine','kidney cells','renal'],temperament:['Mixed'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-6','C-17'],electricity:'G.E.', action_hindi:'गुर्दे की कोशिकाएं + Creatinine।',confidence:80}, 'C-10': {diseases:['diabetes','insulin','sugar','pancreas'],temperament:['Mixed'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['S-2','S-4'],electricity:'Y.E.', action_hindi:'अग्न्याशय + मधुमेह नियंत्रण।',confidence:80}, 'F-1': {diseases:['anxiety','vertigo','insomnia','nervous','fever'],temperament:['Nervous'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['C-2','S-1'],electricity:'W.E.', action_hindi:'तंत्रिका तंत्र + मस्तिष्क शांति।',confidence:85}, 'L-1': {diseases:['platelet','dengue','immunity','wbc'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['A-3','S-1'],electricity:'G.E.', action_hindi:'प्लेटलेट्स + प्रतिरोधक क्षमता।',confidence:80}, 'P-1': {diseases:['lung','bronchitis','cough','respiratory'],temperament:['Lymphatic'], potency:{acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines:['P-3','S-1'],electricity:'Y.E.', action_hindi:'फेफड़े ऊपरी भाग + ब्रोंकाई।',confidence:75}, }; return { code: med.code, name: med.name, group: med.group, system: med.system, ...(FALLBACK[med.code] || { diseases: [med.system.toLowerCase()], temperament: med.group==='A'?['Sanguine']:med.group==='F'?['Nervous']:['Lymphatic'], potency: {acute:'D10',chronic:'D30',hypo:'D4'}, synergy_medicines: ['S-1'], electricity: med.group==='A'?'B.E.':'G.E.', action_hindi: `${med.name} — ${med.system} ki aushadh`, confidence: 50 }), source: 'fallback', }; } function saveKnowledge(knowledge) { fs.writeFileSync(OUTPUT_PATH, JSON.stringify(knowledge, null, 2), 'utf8'); } // Run readAllMedicinesFromBook().catch(err =&gt; { console.error('Error:', err.message); process.exit(1); }); FILE 2: knowledgeBasedSelector.js FILE: backend/services/knowledgeBasedSelector.js medicine_knowledge.json se match karo. Doctor input → 100% correct medicines. Har medicine ka reason explain karo. FILE: backend/services/knowledgeBasedSelector.js // ═══════════════════════════════════════════════════ // KNOWLEDGE-BASED MEDICINE SELECTOR // medicine_knowledge.json se medicines choose karo // ═══════════════════════════════════════════════════ const fs = require('fs'); const path = require('path'); const KNOWLEDGE_PATH = path.join( __dirname, '../data/medicine_knowledge.json'); let _knowledge = null; function loadKnowledge() { if (_knowledge) return _knowledge; try { _knowledge = JSON.parse( fs.readFileSync(KNOWLEDGE_PATH, 'utf8')); console.log(`Medicine knowledge loaded: ${Object.keys(_knowledge).length} medicines`); return _knowledge; } catch(e) { console.warn('medicine_knowledge.json nahi mila'); return {}; } } // ── SCORE: medicine kitni match karti hai ── function scoreMedicine(med, input, temperament) { const { symptoms=[], affected_organs=[], report_values={}, bp_systolic=120, phase='ACUTE' } = input; const symText = symptoms .map(s=&gt;typeof s==='object'?(s.name||''):String(s)) .join(' ').toLowerCase(); let score = 0; const reasons = []; // ── Temperament match ── if (med.temperament?.includes(temperament)) { score += 10; reasons.push(`${temperament} प्रकृति से मेल`); } if (med.temperament?.includes('Mixed')) { score += 4; } // ── Disease match ── (med.diseases||[]).forEach(disease =&gt; { if (symText.includes(disease.toLowerCase())) { score += 8; reasons.push(`लक्षण: ${disease}`); } }); // ── Symptom match ── (med.symptoms||[]).forEach(symptom =&gt; { if (symText.includes(symptom.toLowerCase())) { score += 6; reasons.push(`संकेत: ${symptom}`); } }); // ── Organ match ── (affected_organs||[]).forEach(organ =&gt; { if (med.system?.toLowerCase().includes(organ.toLowerCase())) { score += 8; reasons.push(`अंग: ${organ}`); } }); // ── Report match ── const LAB_KEYS = { uric_acid: ['uric','gout','joint'], creatinine: ['kidney','renal','creatinine'], sugar_fast: ['diabetes','sugar','insulin'], hemoglobin: ['anemia','hemoglobin','blood'], pth: ['bone','calcium','pth'], sgpt: ['liver','hepatitis','jaundice'], cholesterol: ['heart','arteries','bp'], }; Object.entries(report_values||{}).forEach(([k,v]) =&gt; { const ref = { uric_acid:7,creatinine:1.2,sugar_fast:100, hemoglobin:12,pth:65,sgpt:56,cholesterol:200 }; if (!v || !ref[k]) return; const abnormal = (k==='hemoglobin') ? v&lt;ref[k] : v&gt;ref[k]; if (abnormal) { const keywords = LAB_KEYS[k] || []; const matches = keywords.some(kw =&gt; (med.diseases||[]).some(d=&gt;d.includes(kw)) || (med.system||'').toLowerCase().includes(kw)); if (matches) { score += 10; reasons.push(`Report: ${k} = ${v}`); } } }); // ── BP match ── if (bp_systolic &gt;= 140 && (med.diseases||[]).some(d=&gt;d.includes('bp')||d.includes('heart'))) { score += 8; reasons.push(`BP ${bp_systolic}: हृदय/BP दवा`); } return { score, reasons }; } // ── MAIN: Select best medicines ── function selectMedicinesFromKnowledge(input, temperament, isHyper) { const knowledge = loadKnowledge(); if (Object.keys(knowledge).length === 0) { console.warn('Knowledge empty — using fallback'); return null; } const phase = input.phase || 'ACUTE'; // Score all medicines const scored = Object.entries(knowledge).map(([code, med]) =&gt; { const { score, reasons } = scoreMedicine(med, input, temperament); return { code, med, score, reasons }; }); // Sort by score scored.sort((a,b) =&gt; b.score - a.score); // Top medicines const top = scored.filter(m =&gt; m.score &gt; 0).slice(0, 15); console.log('Top medicines:'); top.slice(0,6).forEach(m =&gt; { console.log(` ${m.code}: score=${m.score} | ${m.reasons.slice(0,2).join(', ')}`); }); // Build formulas const allCodes = top.map(m =&gt; m.code); // Potency from medicine knowledge const getPot = (code) =&gt; { const med = knowledge[code]; if (!med?.potency) return isHyper ? 'D10' : 'D4'; if (!isHyper) return med.potency.hypo || 'D4'; return phase==='CHRONIC' ? (med.potency.chronic || 'D30') : (med.potency.acute || 'D10'); }; // Primary potency (from top medicine) const mainPotency = getPot(allCodes[0]); // Formula A: Top 3-4 (temperament primary) const FA_raw = top.slice(0,4).map(m=&gt;m.code); // Formula B: Liver/Kidney/Support — NOT in FA const LIVER_KID = ['S-2','C-6','C-8','S-6','C-17','C-10','S-4']; const FB_raw = []; LIVER_KID.forEach(code =&gt; { if (!FA_raw.includes(code) && allCodes.includes(code) && FB_raw.length &lt; 3) { FB_raw.push(code); } }); // Fill from top if needed allCodes.forEach(code =&gt; { if (!FA_raw.includes(code) && !FB_raw.includes(code) && FB_raw.length &lt; 3) { FB_raw.push(code); } }); // Formula C: Night — different from A+B const NIGHT_PRIORITY = ['F-1','S-1','A-3','C-1']; const FC_raw = []; NIGHT_PRIORITY.forEach(code =&gt; { if (!FA_raw.includes(code) && !FB_raw.includes(code) && FC_raw.length &lt; 3) { FC_raw.push(code); } }); if (FC_raw.length &lt; 2) { allCodes.forEach(code =&gt; { if (!FA_raw.includes(code) && !FB_raw.includes(code) && !FC_raw.includes(code) && FC_raw.length &lt; 3) { FC_raw.push(code); } }); } // Electricity from top medicine's knowledge const topElec = knowledge[FA_raw[0]]?.electricity || 'G.E.'; // Override by disease const symText = (input.symptoms||[]) .map(s=&gt;typeof s==='object'?(s.name||''):String(s)) .join(' ').toLowerCase(); let electricity = topElec; if ((input.bp_systolic||0) &gt;= 140) electricity = 'B.E.'; else if (symText.includes('uric')||symText.includes('jod')) electricity='G.E.'; else if (symText.includes('chakkar')||symText.includes('anxiety')) electricity='W.E.'; else if (!isHyper) electricity = 'R.E.'; // Build reasons text const medReasons = {}; [...FA_raw,...FB_raw,...FC_raw].forEach(code =&gt; { const m = top.find(t=&gt;t.code===code); if (m) medReasons[code] = { action: knowledge[code]?.action_hindi || code, reasons: m.reasons, score: m.score, }; }); return { FA: FA_raw, FB: FB_raw, FC: FC_raw, mainPotency, electricity, medReasons, top_scored: top.slice(0,8), source: 'medicine-knowledge', }; } module.exports = { selectMedicinesFromKnowledge, loadKnowledge }; FILE 3: buildClinicalData() — Knowledge-First ehSourceOfTruthClinical.js mein buildClinicalData() update karo. Pehle knowledge check karo, phir fallback use karo. ehSourceOfTruthClinical.js mein buildClinicalData() replace karo const { selectMedicinesFromKnowledge } = require('./knowledgeBasedSelector'); function buildClinicalData(input) { // STEP 1: Temperament const temperament = input.temperament || detectTemperament(input); // STEP 2: Polarity const polData = detectPolarity(input); const { potency, isHyper } = polData; // STEP 3: Knowledge-based selection const knowledgeResult = selectMedicinesFromKnowledge(input, temperament, isHyper); let FA, FB, FC, electricity, medReasons; if (knowledgeResult) { // ✅ Use book knowledge FA = knowledgeResult.FA; FB = knowledgeResult.FB; FC = knowledgeResult.FC; electricity = knowledgeResult.electricity; medReasons = knowledgeResult.medReasons; console.log('✅ Knowledge-based selection'); } else { // Fallback to rule engine const fallback = selectMedicinesByTemperament(input, temperament); FA = fallback.FA; FB = fallback.FB; FC = fallback.FC; electricity = selectElectricityForFormula(input, isHyper); medReasons = {}; console.log('⚠️ Fallback rule engine'); } // STEP 4: Build formulas with electricity const cPotency = input.phase==='CHRONIC' ? (isHyper?'D30':'D3') : potency; const elecShort = electricity.replace(/\s*\(.*\)/,''); const fmt = (meds, pot) =&gt; { const clean = (Array.isArray(meds)?meds:[meds]) .filter(Boolean) .map(m=&gt;String(m).replace(/-/g,'').replace(/\./g,'').trim()); return `${clean.join(' + ')} ${pot}`; }; const FA_elec = [...FA, electricity]; const formulas = { formula_a: { medicines: FA_elec, formatted: fmt(FA_elec, potency), potency, purpose: `${temperament} — मुख्य रोग + ${electricity}`, timing: 'सुबह खाली पेट + दोपहर पहले + रात पहले', reasons: FA.map(c=&gt;medReasons[c]?.action||c), }, formula_b: { medicines: FB, formatted: fmt(FB, potency), potency, purpose: 'यकृत + गुर्दा + सहायक', timing: 'नाश्ते के बाद + शाम', reasons: FB.map(c=&gt;medReasons[c]?.action||c), }, formula_c: { medicines: FC, formatted: fmt(FC, cPotency), potency: cPotency, purpose: 'रात्रि गहरी चिकित्सा', timing: 'सोते समय', }, formula_d: { medicines: [electricity], formatted: `${elecShort} D3`, potency: 'D3 (बाहरी)', purpose: `बाहरी विद्युत — ${electricity}`, timing: 'सुबह + शाम 20 मिनट', }, }; const labFindings = analyzeLabReports(input.report_values||{}); console.log('='.repeat(45)); console.log(`Temperament : ${temperament}`); console.log(`Polarity : ${polData.polarity} | Potency: ${potency}`); console.log(`Formula A : ${formulas.formula_a.formatted}`); console.log(`Formula B : ${formulas.formula_b.formatted}`); console.log(`Formula C : ${formulas.formula_c.formatted}`); console.log(`Malam D : ${formulas.formula_d.formatted}`); console.log('='.repeat(45)); return { temperament, polData, formulas, electricity, labFindings, medReasons }; } CURSOR FINAL PROMPT — COPY PASTE Yeh poora prompt Cursor mein paste karo. Ollama book padh kar 38 medicines seekhega. Doctor ke patient ke liye 100% sahi formula. === CURSOR: EH BOOK MEDICINE LEARNING === Mujhe ek system chahiye jisme: 1. Ollama book se har medicine ke baare mein seekhe (suru se last tak) 2. medicine_knowledge.json mein save ho 3. Jab doctor patient info dale to knowledge se 100% sahi medicine choose ho === STEP 1: Files banao === FILE 1: backend/scripts/bookMedicineReader.js [Is document ka FILE 1 code] FILE 2: backend/services/knowledgeBasedSelector.js [Is document ka FILE 2 code] FILE 3: ehSourceOfTruthClinical.js buildClinicalData() replace karo [Is document ka FILE 3 code] === STEP 2: package.json script add karo === 'scripts': { 'learn:medicines': 'node backend/scripts/bookMedicineReader.js' } === STEP 3: Pehli baar chalao === Terminal: ollama serve (dusre terminal mein:) npm run learn:medicines Time: ~20-30 minutes Progress dikhega: [1/38] S-1 padh raha hai... ✅ [2/38] S-2 padh raha hai... ✅ ... [38/38] VEN-1 padh raha hai... ✅ Result: backend/data/medicine_knowledge.json bana 38 medicines ka complete knowledge === STEP 4: Verify karo === node -e &quot; const k = require('./backend/data/medicine_knowledge.json'); console.log('S-6:', k['S-6'].diseases); console.log('A-1:', k['A-1'].diseases); console.log('F-1:', k['F-1'].diseases); &quot; Expected: S-6: ['uric acid','kidney','gout',...] A-1: ['high bp','heart','arteries',...] F-1: ['anxiety','vertigo','nervous',...] === STEP 5: Test patient === POST /api/summary/generate Input: age:55, bp:150/95 symptoms:['uric acid','jod dard'] report_values:{uric_acid:8.5} Expected console.log: ✅ Knowledge-based selection Top medicines: S-6: score=26 | uric acid, kidney C-4: score=22 | joint, bone A-1: score=18 | BP, heart Formula A: S6 + C4 + A1 + GE D30 Formula B: S2 + C6 + C8 D30 Formula C: F1 + S1 D30 === END === Step Action Result 1 bookMedicineReader.js banao 38 medicines ka code ready 2 knowledgeBasedSelector.js banao Score-based selection ready 3 npm run learn:medicines medicine_knowledge.json bana 4 buildClinicalData() update Knowledge-first selection 5 Doctor patient dale 100% book-verified formula ✅ Book Padha → 38 Medicines Seekhe → Doctor ke liye 100% Sahi Formula Suru se Last tak | Thoda Thoda | Score-based | Knowledge-first
