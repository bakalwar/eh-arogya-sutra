/** E1 intentionally shares D5's canonical uuid:hex/LF/SHA-256 snapshot contract. */
export {
  buildNormalizationSnapshotFingerprint,
  EMPTY_FACT_VERIFICATION_SNAPSHOT_FINGERPRINT as EMPTY_FACT_ANALYSIS_ACCEPTANCE_SNAPSHOT_FINGERPRINT,
} from './factVerificationSnapshot.js';
