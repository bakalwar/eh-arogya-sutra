[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'Backend API — skip frontend build'"]

[start]
cmd = "node backend/server.js"
