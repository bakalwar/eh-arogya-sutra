# Production (Vercel) — NOT used during local dev (EH_LOCAL_DEV=1).
# Leave API base empty; vercel.json rewrites /api → Railway Node backend.
# Do NOT build with this file when testing locally — use frontend/.env.development instead.
VITE_API_BASE=
VITE_API_URL=
VITE_APP_NAME=E.H. Arogya Sutra
